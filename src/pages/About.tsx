import React from 'react';

const About = () => {

  const divStyle = {
    display: 'flex',
    justifyContent: 'Right',
    alignItems: 'Right',
    height: '100vh'
  };

  return (
    <div style={divStyle}>
      /* add implementation */
    </div>
  );
};
  
export default About;